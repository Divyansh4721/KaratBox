<script src="https://cdn.tailwindcss.com"></script>
<style>
    #header {
        margin-top: -70px;
        opacity: 0;
    }
</style>
<div class="font-sans antialiased">
    <div class="flex items-center justify-center min-h-screen p-4">
        <div class="w-full max-w-sm px-8 py-10 mx-auto bg-slate-800 rounded-2xl shadow-xl">
            <div class="text-center mb-8">
                <h1 class="text-3xl font-bold text-white">
                    GC Stock Software
                </h1>
                <p class="mt-2 text-slate-400">Welcome back! Sign in to your account.</p>
            </div>
            <form action="/users/auth/google">
                <button type="submit"
                    class="w-full flex items-center justify-center gap-3 py-3 px-4 bg-white text-slate-800 font-bold rounded-full shadow-md hover:bg-slate-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-slate-800 focus:ring-white transition-all duration-300 ease-in-out">
                    <img class="w-6 h-6" src="<%= assetPath('images/google.png') %>" alt="">
                    <span>Continue with Google</span>
                </button>
            </form>
        </div>
    </div>
</div>